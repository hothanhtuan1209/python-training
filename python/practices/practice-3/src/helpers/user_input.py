def get_user_input(message_input):
    """
    Get user input with the provided prompt.

    Parameters:
        message_input (str): The message to display to the user.

    Returns:
        str: The user's input as a string.
    """

    return input(message_input)
